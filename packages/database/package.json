{
  "name": "@integration-gateway/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "migrate": "tsx src/migrate.ts"
  },
  "dependencies": {
    "pg": "^8.0.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/pg": "^8.0.0",
    "tsx": "^4.0.0"
  }
}
